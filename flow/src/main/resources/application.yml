spring:
  h2:
    console:
      enabled: true # H2 Console? ??
      path: /h2-console
  datasource:
    driver-class-name: org.h2.Driver # Database? H2? ??
    url: jdbc:h2:~/ticketFlowDB
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true